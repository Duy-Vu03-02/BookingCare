import { MedicalScheduleModel } from './medical-schedule.interface';

export class MedicalScheduleService {
    public static bookingSchedule = async () => {
        try {
        } catch (err) {
            throw err;
        }
    };
}
